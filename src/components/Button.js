import React, { Component } from 'react'
import './button.css';

export default class Button extends Component {
    render() {
    return ( <button type="submit" class="btn btn-light btn-fire">Submit</button>)
    }
}
